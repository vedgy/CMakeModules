include(vedgTools/ExecutableQtStart)
# If USE_QT5 property is set, it overrides FORCE_QT4 option and sets Qt to
# exact version. Including Qt headers, configuring, linking Qt modules becomes
# caller's responsibility.
